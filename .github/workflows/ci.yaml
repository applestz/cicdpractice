name: Deploy ON Push

on:
  push:
    branches:
      - main

jobs:
  check: #job name
    runs-on: ubuntu-latest
    steps:
      - name: Testing #step name
        runs: |
          echo "abcdef1223"